# Data folder

Place raw input files here, or replace this file with a short description of where the data are stored if the data cannot be shared publicly.

A common pattern is:

- `surveys.csv`
- `phenology.csv`
- `spatial_sites.csv`
- `metadata.csv`
